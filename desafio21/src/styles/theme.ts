// Theme colors
const blackColor: string = '#18181b';
const primaryColor: string = '#9333ea';
const primaryColorLight: string = '#a855f7';
const primaryColorExtraLight: string = '#C084FC';
const textColorPrimary: string = '#F9FAFB';
const textColorSecondary: string = '#FFF';
const strokeColor: string = '#3F3F46';

const theme = {
  colors: {
    primary: primaryColor,
    primaryLight: primaryColorLight,
    primaryExtraLigth: primaryColorExtraLight,
    black: blackColor,
    textColorPrimary: textColorPrimary,
    textColorSecondary: textColorSecondary,
    strokeColor: strokeColor,
  },
};

export default theme;
